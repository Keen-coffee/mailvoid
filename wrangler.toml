name = "mailvoid-email-worker"
main = "email-worker.js"
compatibility_date = "2024-12-30"

# Environment variables
[env.production]
vars = { API_ENDPOINT = "https://api.mailvoid.win" }

[env.development]
vars = { API_ENDPOINT = "http://localhost:3000" }

# Routes (optional - only needed if deploying to a specific domain)
# [[routes]]
# pattern = "mail.example.com/*"
# zone_name = "example.com"
